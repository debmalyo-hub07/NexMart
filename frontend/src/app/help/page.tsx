import { HelpCenter } from '@/components/layout/HelpCenter';

export const metadata = { title: 'Shopping help', description: 'Answers about NexMart search, product options, cart, payments, delivery, accounts, and returns.' };
export default function HelpPage() { return <HelpCenter />; }
