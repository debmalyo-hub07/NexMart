import { ShoppingPlanner } from '@/components/planner/ShoppingPlanner';

export const metadata = { title: 'Your shopping planner', description: 'Plan a real basket around your budget, compare options and account for delivery before checkout.' };
export default function PlannerPage() { return <ShoppingPlanner />; }
